const queryReducer = (state = {}, action) => {
    // switch (action.type) {
    //     case 'WRITE': 
    //         return [
    //             ...state, action.payload
    //         ]
    //     default: {
    //         return state;
    //     }
    // }
    return state;
}

export default queryReducer;